---
apiVersion: v1
kind: Service
metadata:
  name: paperless-mariadb
spec:
  type: ClusterIP
  selector:
    app: mariadb
  ports:
    - name: paperless-port
      protocol: TCP
      port: 5432
      targetPort: 5432